>> % Soal nomor 1
>> % Koefisien polinomial (A)
p = [1 2 -1 3];

% Turunan
dp = polyder(p)

dp =

     3     4    -1

>> % Turunan dari hasil perkalian (B)
>> a = [3 6 9];
b = [1 2 0];

% Turunan dari hasil perkalian
dp1 = polyder(a, b);
>> dp1 = polyder(a, b)

dp1 =

    12    36    42    18

>> % Perkalian polinomial
p_total = conv(a, b);

% Turunan dari hasil perkalian
dp2 = polyder(p_total)

dp2 =

    12    36    42    18

>> % Soal nomor 2
>> % Integral dari polinomial
int_y = polyint(p)

int_y =

    0.2500    0.6667   -0.5000    3.0000         0

>> % Soal nomor 3
>> K = 2;
T = 3;
num = [K];
den = [T, 1];
H = tf(num, den)

H =
 
     2
  -------
  3 s + 1
 
Continuous-time transfer function.
Model Properties

>> % Soal nomor 4
>> num = [1, 1];
den = [1, -1, 3];
H = tf(num, den);
t = 0:0.01:10;
step(H, t)

>> % Soal nomor 5
>> K_values = [0.2, 1, 5];
t = 0:0.01:10;
for K = K_values
    H = tf([K], [1, 0]);
    figure;
    step(H, t);
    title(['Step Response for K = ', num2str(K)]);
end

>> % Soal nomor 6
K_values = [0.5, 1, 2];
T = 1;
t = 0:0.01:10;
for K = K_values
    H = tf([K], [T, 1]);
    figure;
    step(H, t);
    title(['Step Response for K = ', num2str(K), ', T = 1']);
end
>> % Step response 2
>> T_values = [0.2, 0.5, 1, 2, 4];
K = 1;
t = 0:0.01:10;
for T = T_values
    H = tf([K], [T, 1]);
    figure;
    step(H, t);
    title(['Step Response for T = ', num2str(T), ', K = 1']);
end
>> % Soal nomor 7
>> A = [0 1;
     -1 -3];          
B = [0 0;
     2 4];             
C = [5 6];
D = [7 0];             

sys = ss(A, B, C, D);

% Step response untuk input u1 dan u2
t = 0:0.01:10;
step(sys, t);

% Konversi ke transfer function
tf_sys = tf(sys);
>> % Soal nomor 8
>> g = 9.81;
m = 8;
r = 5;
b = 10;

A = [0 1;
    -g/r -b/(m*r^2)];
B = [0; 0];
C = eye(2);
D = [0; 0];

sys = ss(A, B, C, D);

% Simulasi respons
t = 0:0.01:10;
initial_state = [0.1; 0];
[y, t, x] = initial(sys, initial_state, t);

plot(t, x);
xlabel('Time (s)');
ylabel('States');
legend('x1', 'x2');
title('Pendulum State Response');
>> % Soal nomor 9
>> m = 2;
k = 5;
c = 1;

A = [0 1/m;
    -k/m -c/m];
B = [0;
     1/m];
C = [1 0];
D = 0;

Ts = 0.1;
sys = ss(A, B, C, D);
sys_d = c2d(sys, Ts);
sys_d

sys_d =
 
  A = 
            x1       x2
   x1   0.9939  0.04867
   x2  -0.2433   0.9452
 
  B = 
             u1
   x1  0.001228
   x2   0.04867
 
  C = 
       x1  x2
   y1   1   0
 
  D = 
       u1
   y1   0
 
Sample time: 0.1 seconds
Discrete-time state-space model.
Model Properties
>> % Soal nomor 10
>> >> % Parameter sistem
m = 1;
c = 1;
k = 50;

% Matriks state-space
A = [0 1;
    -k/m -c/m];
B = [0;
     1/m];
C = [1 0];
D = 0;

% Buat sistem state-space
sys = ss(A, B, C, D);

% Simulasi step response (gaya F(t) sebagai input)
t = 0:0.01:10;
step(sys, t);
title('Step Response of Mass-Spring-Damper System');
xlabel('Time (s)');
ylabel('Displacement x(t)');

% Konversi ke transfer function
tf_sys = tf(sys)s
Transfer Function:

tf_sys =
 
       1
  ------------
  s^2 + s + 50
 
Continuous-time transfer function.
Model Properties
